# Python Web Scraper

## Software version used

* `pip` 25.2
* `python` 3.13

## Setup

Run command `pip install -r requirements.txt` in project root.

## Steps

1. Write list of urls to `urls.txt`.
2. Run python script.
